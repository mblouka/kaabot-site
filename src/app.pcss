/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

html {
  font-family: "Inter Variable", sans-serif;
  @apply bg-black text-zinc-50;
}

button {
  @apply flex items-center gap-2 rounded-3xl bg-inherit px-4 py-2 text-inherit font-semibold bg-green-500 hover:bg-green-400;
}
