/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

html {
  font-family: "Inter Variable", sans-serif;
}

button {
  @apply flex items-center gap-2 rounded-3xl bg-inherit px-4 py-2 text-inherit shadow transition hover:shadow-md;
}
